{% extends "base.html.twig" %}

{% block content %}
    <h1>Restricted area</h1>

    <h2>
        Hi {% if session.user %}{{ session.user.serviceResponse.authenticationSuccess.user }}{%else%}anonymous{% endif %} !
    </h2>

    <p>
        This page is only accessible to authenticated users.
    </p>
{% endblock %}